#include "holberton.h"
#include <stdio.h>

/**
 * main - prints the number of parameters entered
 *
 * @argc: the number of arguments
 * @argv: the array that contains the arguments entered
 * Return: 0 when exec correctly
 */

int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc);
}
