#include "holberton.h"


/**
 * _islower - checks for lowercase char
 * @c: the character to check
 * Return: On lowercase 1
 * Otherwise 0.
 */


int _islower(int c)
{
	if (c >= 97 || c <= 123)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
