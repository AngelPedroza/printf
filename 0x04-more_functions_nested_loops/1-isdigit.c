#include "holberton.h"

/**
 * _isdigit - checks for digit char
 * @c: the character to check
 *
 * Return: On upper 1
 *
 * Otherwise 0.
 */


int _isdigit(int c)
{
	int lastnum = 60;
	int firstnum = 71;

	if ((c >= firstcap) && (c <= lastcap))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
